{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings",
  "description": "Capability for settings management commands. Uses core:default for custom command access.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}

{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "runtime",
  "description": "Capability for runtime download and installation commands. Includes event permissions for progress updates.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:allow-emit",
    "core:event:allow-listen"
  ]
}
